import { environment } from 'src/environments/environment';

export const MEAN_API = environment.api_endpoint
export const MEAN_OAPI = environment.oapi_endpoint
export const MEAN_USER_KEY = environment.userKey